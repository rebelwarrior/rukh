<% provide(:title, "Developer") %>
<% require("kramdown/converter/html") %>
<% if !@user.nil? %>
  <%= Kramdown::Document.new(render('static_pages/versions')).to_html.html_safe %>
  <%= Kramdown::Document.new(render('static_pages/deploy')).to_html.html_safe   %>
  <%= Kramdown::Document.new(render('static_pages/tomcat')).to_html.html_safe   %>
<% end %>
